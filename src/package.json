{
  "name": "functions",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "tsc && npm-run-all build-*",
    "build-resolver": "cd Resolver && npm run build",
    "package": "npm-run-all pk-*",
    "pk-resolver": "cd Resolver && npm run package"
  },
  "keywords": [],
  "license": "MIT-0",
  "dependencies": {
    "npm-run-all": "^4.1.5"
  }
}
